<div class="jumbotron">
    <h1>选择忘记!</h1>
    <p class="lead">生活中越来越多的密码，设置的太简单，害怕被破解，设置的太复杂，自己都忘记了，通过我们使用一个简单的有意义的数字，来记住一个复杂的，无规律的密码.</p>
    <p><a class="btn btn-lg btn-success" href="/password_infos/new" role="button">记住一个</a></p>
  </div>
<form class="form-inline" action="/password_infos" method="get">
    <div class="form-group">
      <input type="text" class="form-control" id="plain_text" name="plain_text" placeholder="一个简单的数字">
    </div>
    <div class="form-group">
      <input type="text" class="form-control" id="pass_desc" name="pass_desc" placeholder="加一点描述">
    </div>
    <button type="submit" class="btn btn-default">Search</button>
  </form>
<br/>
<br/>
  <table class="table table-striped">
    <tr>
        <th>简单数字</th>
        <th>保护密码</th>
        <th>描述</th>
    </tr>
   
      <% @list.each_with_index do |p, index| %>
      <tr>
        <td><%= p.plain_pass %></td>
        <td><%= p.secret_pass %></td>
        <td><%= p.pass_desc %></td>
      </tr>
      <% end %>
  </table>